#!/bin/bash -eu

IMG_PATH_REMOTE='~/CiNetWiFi.png'
IMG_PATH=~/Documents/CiNetWiFi.png
ssh -F $HOBOKIBIN_PATH/.ssh/config bm14 "bash _cinet-wifi $IMG_PATH_REMOTE"
scp -F $HOBOKIBIN_PATH/.ssh/config bm14:$IMG_PATH_REMOTE $IMG_PATH
open $IMG_PATH
